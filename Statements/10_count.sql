-- Cuenta todos los datos especificados

SELECT COUNT(age) FROM users; -- Cuenta los usuarios que tienen registrado la edad

SELECT COUNT(*) FROM users; -- Cuenta todos los usuarios registrados en la tabla